{
  "name": "psa-rules-tests",
  "version": "1.0.0",
  "description": "Firestore and Storage rules tests with Firebase Emulator",
  "main": "index.js",
  "scripts": {
    "test": "node --test --test-concurrency=1"
  },
  "dependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.0",
    "puppeteer-core": "^25.10.0"
  }
}
